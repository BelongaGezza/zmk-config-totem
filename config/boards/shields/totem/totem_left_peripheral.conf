# ── totem_left_peripheral.conf ───────────────────────────────────────────────
# Mode 1: left half as a BLE peripheral only (no central role).
# No ZMK_SPLIT_ROLE_CENTRAL — that belongs to the dongle in this mode.
# All defaults from totem.dtsi and Kconfig.defconfig apply.
